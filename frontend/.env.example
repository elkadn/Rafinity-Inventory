# Local dev (frontend on :5173, backend directly on :8000):
VITE_API_BASE_URL=http://localhost:8000

# Production build behind Caddy (same HTTPS origin serves both frontend
# and API) - leave EMPTY so requests are relative to the current origin:
# VITE_API_BASE_URL=
